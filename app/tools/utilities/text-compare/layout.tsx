import { ReactNode } from "react";

export const metadata = {
  title: "Text Compare Tool – Compare Text Online Free",
  description:
    "Compare text online instantly with this free text compare tool. Detect differences, changes, additions, and edits side-by-side with word-by-word comparison.",
  keywords: [
    "text compare",
    "compare text online",
    "text diff checker",
    "text comparison tool",
    "compare two texts",
    "word by word compare",
    "online diff tool",
  ],
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
