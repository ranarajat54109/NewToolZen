import { ReactNode } from "react";

export const metadata = {
  title: "JSON Viewer – Format and Validate JSON Online Free",
  description:
    "Free JSON Viewer and formatter tool to validate, beautify, and syntax-highlight raw JSON instantly. Perfect for developers, APIs, and debugging.",
  keywords: [
    "json viewer",
    "json formatter",
    "json beautifier",
    "json validator",
    "format json online",
    "beautify json",
    "online json parser",
  ],
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
